# Estructura de datos
## Integrantes: 
- 1 Gian Aedo
- 2 Ariana Blanco
- 3 Adrian Montes

## Instrucciones
 linux / macos:
```bash
rm -rf build && mkdir build && cd build && cmake .. && make && ./SuffixArray && cd ..
```

windows:
```bash
if (Test-Path build) { rmdir build -Recurse -Force }; mkdir build; cd build; cmake ..; cmake --build .; .\SuffixArray.exe; cd ..
```
